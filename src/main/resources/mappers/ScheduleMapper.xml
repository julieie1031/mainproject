<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace = "com.management.dao.ScheduleDAO">
 	<select id="showSchedule" resultType = "ScheduleDTO">
 		SELECT
    			subject,startDate,endDate,memo
    		FROM
    			schedule
    		WHERE
    			id = #{id}
    		ORDER BY
    			num
    		DESC
 	</select>
 	<insert id="addSchedule" parameterType = "ScheduleDTO">
	    	INSERT INTO
	    		schedule(num,subject,startDate,endDate,memo,id)
	    	VALUES
	    		(seq_schedule.nextval,#{subject, jdbcType=VARCHAR},#{startDate, jdbcType=VARCHAR},#{endDate, jdbcType=VARCHAR},#{memo, jdbcType=VARCHAR},#{id,jdbcType=VARCHAR })
 	</insert>
 </mapper>
 