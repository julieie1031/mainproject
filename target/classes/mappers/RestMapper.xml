<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="mappers.RestMapper">
    <!-- 01. 상품 전체 목록 -->
  
    <select id="listRest" resultType="Rest">
        SELECT  
            rest_id AS restId,
            rest_name AS restName,
            rest_address AS restAddress,
            rest_price AS restPrice,
            rest_content AS restContent,
            rest_image AS restImage
        FROM rest 
        ORDER BY rest_name
    </select>
    <!-- 02. 상품 상세보기 -->
    <select id="detailRest" parameterType="String" resultType="Rest">
        SELECT
            rest_id AS restId,
            rest_name AS restName,
            rest_number AS restNumber,
            rest_price AS restPrice,
            rest_content AS restContent,
            rest_image AS restImage
        FROM rest 
        WHERE  rest_id = #{restId}
    </select>
</mapper>	